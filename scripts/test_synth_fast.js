const { generateHistoricalSynthesis } = require('../frontend/lib/cloud-ai.ts');

async function run() {
  console.log('Testing synthesis function...');
  const t0 = Date.now();
  // Compile/run through ts or test fetch
}
